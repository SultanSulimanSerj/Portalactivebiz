# Идеи для продакшена

> Функционал для коммерческого запуска SaaS-платформы
>
> *Составлено: Январь 2026*

---

## 🔐 1. Система регистрации и управления пользователями

### 1.1. Убрать публичную регистрацию

**Текущая ситуация:**
- Любой может зарегистрироваться через `/auth/signup`
- Создаётся новая компания и пользователь с ролью OWNER

**Что нужно сделать:**
- [ ] Отключить публичную регистрацию (`/auth/signup` → редирект или 404)
- [ ] Создать админ-панель для менеджеров платформы
- [ ] Только менеджер платформы может создать аккаунт для директора компании

**Новая модель:**
```
Менеджер платформы → создаёт → Директор компании (OWNER)
Директор компании → создаёт → Сотрудники (ADMIN/MANAGER/USER)
```

**Реализация:**
- [ ] Новая роль `PLATFORM_MANAGER` (супер-админ)
- [ ] API `/api/platform/companies` — создание компаний (только для PLATFORM_MANAGER)
- [ ] API `/api/platform/companies/[id]/create-owner` — создание директора компании
- [ ] Страница `/platform/companies` — список компаний и создание новых

---

### 1.2. Директор создаёт сотрудников

**Текущая ситуация:**
- OWNER и ADMIN могут создавать пользователей через `/api/users`
- Есть API `/api/auth/invite` для приглашений

**Что нужно улучшить:**
- [ ] OWNER может создавать пользователей с любыми ролями (кроме OWNER)
- [ ] OWNER может назначать должности (`position`)
- [ ] OWNER может изменять роли существующих пользователей
- [ ] При создании пользователя отправляется email с временным паролем
- [ ] При первом входе пользователь должен сменить пароль

**Реализация:**
- [ ] Улучшить UI на странице `/users` для OWNER
- [ ] Добавить модальное окно создания пользователя с выбором роли и должности
- [ ] Интеграция с email-сервисом (nodemailer/SendGrid) для отправки приглашений
- [ ] Флаг `mustChangePassword` в модели User

---

## 💳 2. Система подписок и оплат

### 2.1. Модель подписки

**Новая модель в БД:**
```prisma
model Subscription {
  id              String   @id @default(cuid())
  companyId       String   @unique
  company         Company  @relation(fields: [companyId], references: [id])
  
  plan            SubscriptionPlan @default(BASIC)
  status          SubscriptionStatus @default(TRIAL)
  currentPeriodStart DateTime
  currentPeriodEnd   DateTime
  
  // Оплата
  lastPaymentDate    DateTime?
  nextPaymentDate    DateTime?
  amount             Decimal @db.Decimal(10, 2)
  currency           String  @default("RUB")
  
  // Автопродление
  autoRenew          Boolean @default(true)
  cancelAtPeriodEnd  Boolean @default(false)
  
  // История
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
  canceledAt         DateTime?
  
  payments           Payment[]
}

enum SubscriptionPlan {
  TRIAL     // Пробный период (14 дней)
  BASIC     // Базовый тариф
  STANDARD  // Стандартный тариф
  PREMIUM   // Премиум тариф
}

enum SubscriptionStatus {
  TRIAL           // Пробный период
  ACTIVE          // Активна и оплачена
  PAST_DUE        // Просрочка оплаты
  CANCELED        // Отменена
  SUSPENDED       // Приостановлена (блокировка)
}

model Payment {
  id              String   @id @default(cuid())
  subscriptionId  String
  subscription    Subscription @relation(fields: [subscriptionId], references: [id])
  
  amount          Decimal @db.Decimal(10, 2)
  currency        String  @default("RUB")
  status          PaymentStatus @default(PENDING)
  
  // Детали платежа
  paymentMethod   String?  // "card", "bank_transfer", "invoice"
  transactionId   String?  // ID транзакции в платёжной системе
  invoiceNumber   String?  // Номер счёта
  
  // Даты
  paidAt          DateTime?
  dueDate         DateTime
  createdAt       DateTime @default(now())
  
  // Метаданные
  metadata        Json?    // Дополнительные данные от платёжной системы
}

enum PaymentStatus {
  PENDING      // Ожидает оплаты
  PROCESSING   // В обработке
  COMPLETED    // Оплачено
  FAILED       // Ошибка оплаты
  REFUNDED     // Возврат
}
```

**Тарифные планы:**
| План | Цена/мес | Проектов | Пользователей | Функции |
|------|----------|----------|---------------|---------|
| TRIAL | Бесплатно | 3 | 5 | Все функции (14 дней) |
| BASIC | 5 000 ₽ | 10 | 10 | Базовые функции |
| STANDARD | 15 000 ₽ | 50 | 30 | Все функции + приоритетная поддержка |
| PREMIUM | 30 000 ₽ | Безлимит | Безлимит | Все функции + персональный менеджер |

---

### 2.2. Отслеживание оплат и блокировка доступа

**Логика блокировки:**
1. При создании компании → статус `TRIAL`, пробный период 14 дней
2. За 3 дня до окончания пробного периода → уведомление директору
3. После окончания пробного периода без оплаты → статус `PAST_DUE`
4. Через 7 дней после `PAST_DUE` → статус `SUSPENDED`, блокировка доступа
5. После оплаты → статус `ACTIVE`, разблокировка

**Реализация:**
- [ ] Cron-задача для проверки подписок (ежедневно в 9:00)
- [ ] API `/api/subscriptions/check-status` — проверка статусов всех подписок
- [ ] Middleware для проверки блокировки компании при каждом запросе
- [ ] Страница `/billing` для директора компании (просмотр подписки, оплата)
- [ ] Страница блокировки `/suspended` — показывается всем пользователям заблокированной компании

**Код middleware:**
```typescript
// src/middleware/subscription-check.ts
export async function checkCompanySubscription(companyId: string) {
  const subscription = await prisma.subscription.findUnique({
    where: { companyId },
    include: { company: true }
  })
  
  if (!subscription) return { allowed: false, reason: 'NO_SUBSCRIPTION' }
  
  // Блокировка при SUSPENDED или PAST_DUE > 7 дней
  if (subscription.status === 'SUSPENDED') {
    return { allowed: false, reason: 'SUSPENDED' }
  }
  
  if (subscription.status === 'PAST_DUE') {
    const daysPastDue = differenceInDays(new Date(), subscription.currentPeriodEnd)
    if (daysPastDue > 7) {
      // Автоматически переводим в SUSPENDED
      await prisma.subscription.update({
        where: { id: subscription.id },
        data: { status: 'SUSPENDED' }
      })
      return { allowed: false, reason: 'SUSPENDED' }
    }
  }
  
  return { allowed: true }
}
```

---

### 2.3. Интеграция с платёжными системами

**Варианты интеграции:**
1. **ЮKassa (Яндекс.Касса)** — для России
   - Приём карт, СБП, электронные кошельки
   - API: `@yandex-money/yandex-checkout-sdk`

2. **CloudPayments** — для России
   - Приём карт, рекуррентные платежи
   - API: `cloudpayments-sdk`

3. **Stripe** — международные платежи
   - Приём карт по всему миру
   - API: `stripe`

**Реализация:**
- [ ] Выбор платёжной системы в настройках платформы
- [ ] API `/api/payments/create` — создание платежа
- [ ] Webhook для обработки уведомлений от платёжной системы
- [ ] API `/api/payments/webhook/[provider]` — обработка webhook'ов
- [ ] Страница успешной оплаты `/payment/success`
- [ ] Страница ошибки оплаты `/payment/error`

---

## 🛡️ 3. Безопасность и мониторинг

### 3.1. Усиление безопасности

**Что нужно добавить:**
- [ ] Rate limiting для API (защита от DDoS и брутфорса)
- [ ] Логирование всех попыток входа (успешных и неудачных)
- [ ] Блокировка IP после 5 неудачных попыток входа за 15 минут
- [ ] Двухфакторная аутентификация (2FA) для OWNER и ADMIN
- [ ] Политика паролей (минимум 8 символов, буквы + цифры)
- [ ] HTTPS только (redirect HTTP → HTTPS)
- [ ] CORS настройки для production
- [ ] Защита от SQL-инъекций (Prisma уже защищает, но проверить)
- [ ] Валидация всех входных данных (Zod схемы)

**Реализация:**
- [ ] Библиотека `express-rate-limit` или `@upstash/ratelimit`
- [ ] Модель `LoginAttempt` для логирования
- [ ] Модель `BlockedIP` для блокировки IP
- [ ] Библиотека `speakeasy` для 2FA (TOTP)
- [ ] Middleware для проверки сложности пароля

---

### 3.2. Мониторинг и логирование

**Что нужно добавить:**
- [ ] Централизованное логирование (Winston/Pino)
- [ ] Отслеживание ошибок (Sentry)
- [ ] Мониторинг производительности (New Relic/DataDog)
- [ ] Логирование всех критических действий (создание/удаление пользователей, изменение ролей, финансовые операции)
- [ ] Алерты при критических ошибках (email/SMS)

**Реализация:**
- [ ] Интеграция Sentry: `@sentry/nextjs`
- [ ] Структурированные логи в JSON формате
- [ ] Ротация логов (ежедневно)
- [ ] Dashboard для просмотра логов (или интеграция с внешним сервисом)

---

## 📊 4. Аналитика и отчётность

### 4.1. Аналитика использования

**Что отслеживать:**
- Количество активных пользователей по компаниям
- Количество проектов, задач, документов
- Использование функций (какие модули популярны)
- Время работы в системе
- География пользователей

**Реализация:**
- [ ] Модель `AnalyticsEvent` для событий
- [ ] API `/api/analytics/track` — отправка событий
- [ ] Dashboard для платформы `/platform/analytics`
- [ ] Экспорт данных в CSV/Excel

---

### 4.2. Отчёты для директоров компаний

**Что добавить:**
- Отчёт по активности сотрудников
- Отчёт по проектам (статусы, прогресс)
- Финансовый отчёт (доходы/расходы)
- Отчёт по задачам (выполненные/просроченные)

**Реализация:**
- [ ] Страница `/reports` с выбором типа отчёта
- [ ] Генерация отчётов в PDF/Excel
- [ ] Возможность запланировать автоматическую отправку отчётов

---

## 🔧 5. Техническая инфраструктура

### 5.1. Резервное копирование

**Что нужно:**
- [ ] Автоматическое ежедневное резервное копирование БД
- [ ] Хранение бэкапов в облаке (S3/Yandex Object Storage)
- [ ] Возможность восстановления из бэкапа
- [ ] Тестирование восстановления (раз в месяц)

**Реализация:**
- [ ] Cron-задача для бэкапа PostgreSQL (`pg_dump`)
- [ ] Загрузка в S3 через AWS SDK
- [ ] Страница `/platform/backups` для управления бэкапами

---

### 5.2. Масштабирование

**Что нужно:**
- [ ] Горизонтальное масштабирование (несколько инстансов Next.js)
- [ ] Кэширование (Redis)
- [ ] CDN для статических файлов
- [ ] Оптимизация запросов к БД (индексы, connection pooling)
- [ ] Очереди для фоновых задач (Bull/BullMQ)

**Реализация:**
- [ ] Redis для сессий и кэша
- [ ] PM2 или Kubernetes для управления процессами
- [ ] Cloudflare или CloudFront для CDN

---

### 5.3. CI/CD

**Что нужно:**
- [ ] Автоматические тесты (Jest/Vitest)
- [ ] Автоматический деплой при push в main
- [ ] Staging окружение для тестирования
- [ ] Rollback при ошибках

**Реализация:**
- [ ] GitHub Actions или GitLab CI
- [ ] Автоматический запуск тестов
- [ ] Автоматический деплой на сервер

---

## 📧 6. Email и уведомления

### 6.1. Email-сервис

**Что нужно:**
- [ ] Настроить отправку email (SendGrid/Mailgun/Postmark)
- [ ] Шаблоны писем (приветствие, приглашение, уведомления)
- [ ] Очередь для отправки email (чтобы не блокировать запросы)

**Типы писем:**
- Приветственное письмо новому пользователю
- Приглашение в компанию
- Уведомление о просрочке подписки
- Уведомление о блокировке
- Еженедельный дайджест активности

**Реализация:**
- [ ] Интеграция SendGrid: `@sendgrid/mail`
- [ ] Шаблоны в `src/templates/emails/`
- [ ] Очередь через Bull/BullMQ

---

## 🎨 7. UX улучшения для продакшена

### 7.1. Onboarding

**Что добавить:**
- [ ] Тур по интерфейсу для новых пользователей
- [ ] Подсказки при первом использовании функций
- [ ] Видео-туториалы (опционально)

**Реализация:**
- [ ] Библиотека `react-joyride` для туров
- [ ] Флаг `hasCompletedOnboarding` в User

---

### 7.2. Обратная связь

**Что добавить:**
- [ ] Форма обратной связи в интерфейсе
- [ ] Кнопка "Сообщить об ошибке"
- [ ] Чат поддержки (интеграция с Intercom/Crisp)

---

## 📋 Сводная таблица приоритетов

| # | Функция | Приоритет | Сложность | Время |
|---|---------|-----------|-----------|-------|
| 1 | Убрать публичную регистрацию | 🔴 Критично | Низкая | 1 день |
| 2 | Система подписок (модель БД) | 🔴 Критично | Средняя | 3 дня |
| 3 | Блокировка при просрочке | 🔴 Критично | Средняя | 2 дня |
| 4 | Интеграция платёжной системы | 🔴 Критично | Высокая | 5 дней |
| 5 | Email-уведомления | 🟡 Высокий | Средняя | 3 дня |
| 6 | Безопасность (rate limiting, 2FA) | 🟡 Высокий | Средняя | 4 дня |
| 7 | Мониторинг и логирование | 🟡 Высокий | Средняя | 3 дня |
| 8 | Резервное копирование | 🟡 Высокий | Низкая | 2 дня |
| 9 | Аналитика | 🟢 Средний | Средняя | 4 дня |
| 10 | Onboarding | 🟢 Средний | Низкая | 2 дня |

---

## 🚀 План запуска в продакшен

### Фаза 1: Критичный функционал (2 недели)
1. ✅ Убрать публичную регистрацию
2. ✅ Создать систему подписок
3. ✅ Реализовать блокировку при просрочке
4. ✅ Интегрировать платёжную систему (минимум одну)
5. ✅ Настроить email-уведомления

### Фаза 2: Безопасность и стабильность (1 неделя)
6. ✅ Rate limiting и защита от брутфорса
7. ✅ Мониторинг и логирование
8. ✅ Резервное копирование

### Фаза 3: Улучшения (1 неделя)
9. ✅ Аналитика использования
10. ✅ Onboarding для новых пользователей
11. ✅ Обратная связь и поддержка

**Итого: ~4 недели до готовности к продакшену**

---

*Последнее обновление: Январь 2026*
