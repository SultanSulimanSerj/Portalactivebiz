# 🚀 Manexa - Функционал системы (на основе кода)

## 📋 Описание проекта

**Manexa** — это современная SaaS-платформа для комплексного управления проектами, документооборотом, финансами и командой. Система разработана для строительных компаний, проектных бюро и других организаций, работающих с проектами и объектами.

---

## ✨ Функционал системы (реализовано в коде)

### 1️⃣ **Управление проектами** (`src/app/api/projects/route.ts`)

#### Создание проекта (POST `/api/projects`):
**Реализация:** Строки 134-276
- ✅ Создание проекта с полными реквизитами клиента (15 полей)
- ✅ Автоматическое создание финансовой записи при указании бюджета
- ✅ Определение типа финансовой записи по статусу проекта:
  - `PLANNED_INCOME` - для статусов PLANNING, ON_HOLD
  - `INCOME` - для статусов ACTIVE, COMPLETED
- ✅ Транзакционная безопасность (Prisma `$transaction`)
- ✅ Валидация обязательных полей (название проекта)
- ✅ Автоматическая генерация ID через `generateId()`

**Реквизиты клиента в проекте:**
- `clientName`, `clientLegalName` - названия организации
- `clientInn`, `clientKpp`, `clientOgrn` - реквизиты
- `clientLegalAddress`, `clientActualAddress` - адреса
- `clientDirectorName` - ФИО директора
- `clientContactPhone`, `clientContactEmail` - контакты
- `clientBankAccount`, `clientBankBik`, `clientBankName`, `clientCorrespondentAccount` - банковские реквизиты

#### Получение списка проектов (GET `/api/projects`):
**Реализация:** Строки 9-132
- ✅ Пагинация (page, limit)
- ✅ Фильтрация по статусу и поиск по названию/описанию
- ✅ Фильтрация по ролям:
  - **OWNER/ADMIN** - видят все проекты компании (строки 30-37)
  - **MANAGER/USER** - только свои проекты или где участники (строки 38-61)
- ✅ Автоматический расчёт финансовых показателей для каждого проекта:
  - `income` - реальный доход (INCOME)
  - `plannedIncome` - планируемый доход (PLANNED_INCOME)
  - `expenses` - расходы (EXPENSE)
  - `profit` - прибыль (income - expenses)
  - `margin` - маржа в % (profit / income × 100)
- ✅ Подсчёт связанных сущностей (_count: tasks, documents, users)
- ✅ Сортировка по дате создания (desc)

#### Фильтрация доступа:
**Код:** Строки 23-61
```typescript
// OWNER и ADMIN видят все проекты компании
if (user.role === UserRole.OWNER || user.role === UserRole.ADMIN) {
  // Никаких дополнительных ограничений
} else {
  // MANAGER и USER видят только проекты, где являются участниками
  where.OR = [
    { creatorId: user.id }, // Пользователь создал проект
    { users: { some: { userId: user.id } } } // Пользователь является участником
  ]
}
```

---

### 2️⃣ **Система задач** (`src/app/api/tasks/route.ts`)

#### Создание задачи (POST `/api/tasks`):
**Реализация:** Строки 95-161
- ✅ Создание задачи с описанием, статусом, приоритетом, сроком
- ✅ Множественное назначение исполнителей (`assigneeIds` - массив)
- ✅ Автоматическое создание связей `TaskAssignment` для каждого исполнителя
- ✅ Автоматическая отправка уведомлений назначенным пользователям через `notifyTaskAssignment()`
- ✅ Привязка к проекту (опционально)
- ✅ Включение данных проекта и создателя в ответ

**Код создания назначений:**
```typescript
assignments: {
  create: assigneeIds?.map((userId: string) => ({
    id: generateId(),
    userId
  })) || []
}
```

#### Получение списка задач (GET `/api/tasks`):
**Реализация:** Строки 8-93
- ✅ Пагинация и фильтрация по статусу, проекту, поиск
- ✅ Фильтрация по ролям:
  - **OWNER/ADMIN** - все задачи компании (строки 39-40)
  - **MANAGER** - задачи проектов, где участник (строки 41-46)
  - **USER** - только назначенные задачи или созданные им (строки 48-53)
- ✅ Включение данных проекта, создателя и всех назначений
- ✅ Сортировка по дате создания (desc)

**Фильтрация для USER:**
```typescript
where.OR = [
  { assignments: { some: { userId: user.id } } }, // Назначенные задачи
  { creatorId: user.id } // Созданные пользователем задачи
]
```

#### Уведомления о назначении:
**Реализация:** Строки 142-154
- Автоматическая отправка уведомлений через `notifyTaskAssignment()`
- Уведомления содержат название задачи и проект
- Обработка ошибок уведомлений не прерывает создание задачи

---

### 3️⃣ **Генерация документов** (`src/app/api/documents/generate/route.ts`)

#### Генерация договоров подряда (POST `/api/documents/generate`):
**Реализация:** Строки 9-196
- ✅ Автоматическое получение данных проекта с компанией, создателем и сметами
- ✅ Проверка доступа к проекту (принадлежность к компании пользователя)
- ✅ Формирование данных договора из:
  - Реквизитов компании-исполнителя (17 полей)
  - Реквизитов клиента-заказчика (13 полей)
  - Данных проекта (название, описание, адрес работ, сроки)
  - Данных сметы (сумма с НДС или без)
- ✅ Генерация через библиотеку `docx` (Packer.toBuffer)
- ✅ Автоматическое создание папки `uploads` если не существует
- ✅ Сохранение файла на диск
- ✅ Создание записи в БД с категорией `CONTRACT`
- ✅ Автоматическая публикация документа (`isPublished: true`)
- ✅ Возврат файла для скачивания с заголовками

**Код формирования данных:**
```typescript
const contractData: ContractData = {
  contractNumber: `DOG-${Date.now()}`,
  contractDate: today.toLocaleDateString('ru-RU'),
  // Исполнитель (наша компания) - 17 полей
  executorName: project.company?.name || '',
  executorLegalName: project.company?.legalName || '',
  // ... и т.д.
  // Заказчик (клиент) - 13 полей
  clientName: project.clientName || '',
  // ... и т.д.
  // Данные проекта
  totalAmount: estimate 
    ? Number(estimate.totalWithVat || estimate.total).toLocaleString('ru-RU') 
    : (project.budget ? Number(project.budget).toLocaleString('ru-RU') : ''),
  vatEnabled: estimate ? estimate.vatEnabled : false
}
```

**Генерация документа:**
- Использует `generateContractDocument()` из `src/lib/document-generator.ts`
- 13 разделов договора автоматически заполняются
- Формат: `.docx` (Microsoft Word)

---

### 4️⃣ **Сметы** (`src/app/api/projects/[id]/estimates/route.ts`)

#### Создание сметы (POST `/api/projects/[id]/estimates`):
**Реализация:** Строки 49-113
- ✅ Проверка прав доступа (`canCreateEstimates`)
- ✅ Проверка доступа к проекту через `canUserAccessProject()`
- ✅ Валидация названия сметы (обязательное поле)
- ✅ Проверка существования проекта и принадлежности к компании
- ✅ Создание сметы с начальными значениями:
  - `total: 0`, `totalCost: 0`, `profit: 0`
  - `vatEnabled: false`, `vatRate: 20`, `vatAmount: 0`, `totalWithVat: 0`
- ✅ Привязка к проекту и создателю
- ✅ Возврат сметы с позициями и данными создателя

#### Получение смет проекта (GET `/api/projects/[id]/estimates`):
**Реализация:** Строки 7-47
- ✅ Проверка прав доступа (`canViewEstimates`)
- ✅ Проверка доступа к проекту
- ✅ Фильтрация по `projectId` и `companyId`
- ✅ Включение всех позиций (`items`) и данных создателя
- ✅ Сортировка по дате создания (desc)

**Модель Estimate** (`prisma/schema.prisma:654-703`):
- Автоматические поля: `total`, `totalCost`, `profit`
- НДС: `vatEnabled`, `vatRate`, `vatAmount`, `totalWithVat`
- Связь с позициями: `EstimateItem[]`
- Использование в генерации договоров (сумма берётся из сметы)

---

### 5️⃣ **Финансовый учёт** (`src/app/api/finance/kpi/route.ts`)

#### Финансовые KPI (GET `/api/finance/kpi`):
**Реализация:** Строки 6-89
- ✅ Проверка прав доступа (`canViewFinances`)
- ✅ Фильтрация по проекту (опционально) и компании
- ✅ Получение всех финансовых операций с данными проекта
- ✅ Автоматический расчёт показателей:
  - `plannedIncome` - планируемый доход (PLANNED_INCOME)
  - `actualIncome` - реальный доход (INCOME)
  - `actualExpenses` - фактические расходы (EXPENSE)
  - `budgetPlan` - план бюджета (из проекта или plannedIncome)
  - `expensePlan` - план расходов (80% от бюджета)
  - `deviation` - отклонение расходов от плана в %
  - `deviationAmount` - сумма отклонения
  - `incomeByActs` - доходы по актам

**Расчёт отклонения:**
```typescript
const deviationAmount = actualExpenses > expensePlan 
  ? actualExpenses - expensePlan 
  : 0
const deviation = expensePlan > 0 
  ? (deviationAmount / expensePlan) * 100 
  : 0
```

#### Расчёт маржи в проектах (`src/app/api/projects/route.ts:89-104`):
**Реализация:** Для каждого проекта автоматически рассчитывается:
- `income` - реальный доход (только INCOME)
- `plannedIncome` - планируемый доход (PLANNED_INCOME)
- `expenses` - расходы (EXPENSE)
- `profit` - прибыль (income - expenses)
- `margin` - маржа в % (profit / income × 100), округление до 1 знака

**Типы финансовых операций** (`prisma/schema.prisma:586-590`):
- `INCOME` - реальные доходы
- `EXPENSE` - расходы
- `PLANNED_INCOME` - планируемые доходы

#### Автоматическое создание финансовой записи при создании проекта:
**Реализация:** `src/app/api/projects/route.ts:222-248`
- При указании бюджета автоматически создаётся финансовая запись
- Тип зависит от статуса проекта:
  - `PLANNED_INCOME` - для PLANNING, ON_HOLD
  - `INCOME` - для ACTIVE, COMPLETED

---

### 6️⃣ **Система согласований** (`src/app/api/approvals/[id]/respond/route.ts`)

#### Ответ на согласование (POST `/api/approvals/[id]/respond`):
**Реализация:** Строки 7-188
- ✅ Проверка аутентификации пользователя
- ✅ Валидация статуса (APPROVED или REJECTED)
- ✅ Проверка, что пользователь является участником согласования
- ✅ Проверка принадлежности к компании
- ✅ Обновление статуса назначения (`ApprovalAssignment`)
- ✅ Автоматическое определение общего статуса согласования:
  - Если `requireAllApprovals: true` - требуется согласие всех
  - Если `requireAllApprovals: false` - достаточно одного согласия
- ✅ Обновление статуса согласования (APPROVED/REJECTED)
- ✅ Автоматическая публикация документа при одобрении (`autoPublishOnApproval`)
- ✅ Автоматическое создание документов из вложений согласования
- ✅ Запись в историю (`ApprovalHistory`)

**Логика определения статуса:**
```typescript
if (approval.requireAllApprovals) {
  // Требуется согласие всех
  if (allApproved) newApprovalStatus = 'APPROVED'
  else if (anyRejected) newApprovalStatus = 'REJECTED'
} else {
  // Достаточно одного согласия
  if (anyRejected) newApprovalStatus = 'REJECTED'
  else if (someApproved) newApprovalStatus = 'APPROVED'
}
```

**Автоматическая публикация документа:**
```typescript
if (newApprovalStatus === 'APPROVED' && approval.document && approval.autoPublishOnApproval) {
  await prisma.document.update({
    where: { id: approval.documentId! },
    data: { isPublished: true, publishedAt: new Date() }
  })
}
```

**Создание документов из вложений:**
- При одобрении все вложения автоматически создаются как документы проекта
- Категория: `LEGAL`
- Автоматическая публикация

---

### 7️⃣ **Чат проектов** (`src/app/api/projects/[id]/messages/route.ts`)

#### Отправка сообщения (POST `/api/projects/[id]/messages`):
**Реализация:** Строки 52-162
- ✅ Проверка аутентификации и доступа к проекту
- ✅ Валидация содержимого сообщения (обязательное поле)
- ✅ Создание сообщения в БД с привязкой к проекту
- ✅ Поддержка упоминаний (`mentions` - массив ID пользователей)
- ✅ Отправка через WebSocket всем участникам проекта:
  - Событие `new-message` в комнату `project:${projectId}`
  - Данные: сообщение, пользователь, упоминания
- ✅ Отправка уведомлений упомянутым пользователям через `notifyMentionInChat()`
- ✅ Возврат созданного сообщения с данными пользователя

**WebSocket отправка:**
```typescript
if (global.io) {
  global.io.to(`project:${params.id}`).emit('new-message', {
    message,
    mentions: mentions || []
  })
  
  // Уведомления упомянутым пользователям
  if (mentions && mentions.length > 0) {
    mentions.forEach((mentionedUserId: string) => {
      global.io.to(`user:${mentionedUserId}`).emit('notification', {
        type: 'mention',
        message: `Вас упомянули в проекте "${project.name}"`
      })
    })
  }
}
```

#### Получение истории сообщений (GET `/api/projects/[id]/messages`):
**Реализация:** Строки 8-50
- ✅ Проверка доступа к проекту
- ✅ Получение последних 100 сообщений
- ✅ Включение данных пользователя (id, name, email)
- ✅ Сортировка по дате создания (asc - от старых к новым)

#### WebSocket сервер (`server.js`):
**Реализация:** Строки 26-71
- ✅ Инициализация Socket.IO с CORS
- ✅ Глобальный доступ через `global.io` для API роутов
- ✅ Комнаты проектов: `join-project`, `leave-project`
- ✅ Персональные комнаты: `join-user`
- ✅ Индикатор печати: событие `typing`

---

### 8️⃣ **Система уведомлений** (`src/lib/notifications.ts`)

#### Уведомления о назначении на задачи (`notifyTaskAssignment`):
**Реализация:** Строки 82-100
- ✅ Создание уведомлений для всех назначенных пользователей
- ✅ Тип: `INFO`
- ✅ Содержание: название задачи и проект
- ✅ Привязка к проекту и задаче (`actionType: 'task'`, `actionId: taskId`)

#### Уведомления для проекта (`createProjectNotification`):
**Реализация:** Строки 23-51
- ✅ Получение всех участников проекта
- ✅ Создание уведомлений для всех участников параллельно (`Promise.all`)
- ✅ Тип уведомления настраиваемый (info, success, warning, error)
- ✅ Привязка к проекту

#### Системные уведомления (`createSystemNotification`):
**Реализация:** Строки 53-79
- ✅ Получение всех пользователей компании
- ✅ Создание уведомлений для всех пользователей
- ✅ Использование для важных системных событий

#### Типы уведомлений (`prisma/schema.prisma:592-597`):
- `INFO` - информационные
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `SUCCESS` - успешные действия

**Модель Notification:**
- `title`, `message` - содержимое
- `type` - тип уведомления
- `isRead` - статус прочтения
- `actionType`, `actionId` - привязка к объекту (task, project, document)
- `projectId` - привязка к проекту

---

### 9️⃣ **Дашборд** (`src/components/dashboard.tsx`)

#### Получение статистики (`fetchDashboardData`):
**Реализация:** Строки 48-71
- ✅ Параллельные запросы через `Promise.all`:
  - `/api/projects` - список проектов
  - `/api/tasks` - список задач
  - `/api/documents` - список документов
- ✅ Расчёт статистики:
  - `totalProjects` - общее количество проектов
  - `activeProjects` - активные проекты (статус ACTIVE)
  - `totalTasks` - общее количество задач
  - `totalDocuments` - общее количество документов

#### Карточки статистики (`statCards`):
**Реализация:** Строки 138-167
- ✅ 4 карточки с иконками и ссылками:
  - Всего проектов (FolderOpen, синий)
  - Активные проекты (Activity, зелёный)
  - Всего задач (CheckCircle, фиолетовый)
  - Документы (FileText, оранжевый)
- ✅ Кликабельные карточки с переходом к разделам

#### Быстрые действия (`quickActions`):
**Реализация:** Строки 169-202
- ✅ 4 быстрых действия с проверкой прав через `PermissionGuard`:
  - Создать проект (`canCreateProjects`)
  - Добавить задачу (`canCreateTasks`)
  - Загрузить документ (`canCreateDocuments`)
  - Финансовый отчёт (`canViewReports`)
- ✅ Каждое действие имеет иконку, описание и ссылку

#### Последняя активность (`fetchActivity`):
**Реализация:** Строки 73-85
- ✅ Запрос к `/api/activity?limit=10`
- ✅ Отображение последних 10 действий
- ✅ Форматирование времени ("Только что", "5 мин. назад", и т.д.)
- ✅ Иконки и цвета по типу активности
- ✅ Отображение пользователя, действия и объекта

**Форматирование времени:**
```typescript
if (diffInSeconds < 60) return 'Только что'
if (diffInSeconds < 3600) return `${Math.floor(diffInSeconds / 60)} мин. назад`
if (diffInSeconds < 86400) return `${Math.floor(diffInSeconds / 3600)} ч. назад`
```

---

### 🔟 **Управление пользователями и компанией**

#### Роли пользователей:
1. **OWNER (Владелец)**
   - Полный доступ ко всем функциям
   - Управление компанией и настройками
   - Просмотр всех данных компании

2. **ADMIN (Администратор)**
   - Управление пользователями и проектами
   - Доступ ко всем данным компании
   - Настройка системы

3. **MANAGER (Менеджер)**
   - Создание проектов и управление ими
   - Доступ только к своим проектам
   - Управление задачами и документами

4. **USER (Пользователь)**
   - Работа с назначенными задачами
   - Просмотр проектов, где является участником
   - Ограниченный доступ

#### Многопользовательская система:
- ✅ Изоляция данных по компаниям
- ✅ Каждая компания видит только свои данные
- ✅ Гибкая система прав доступа
- ✅ Управление участниками проектов
- ✅ Приглашение новых пользователей (в разработке)

#### Данные компании:
- Полное и краткое название
- ИНН, КПП, ОГРН
- Юридический и фактический адреса
- Банковские реквизиты
- Контактная информация
- Данные директора
- Настройки нумерации документов

---

## 🛠 Технологический стек

### Frontend:
- ⚛️ **Next.js 14** — React фреймворк с SSR и App Router
- 🎨 **TypeScript** — типизированный JavaScript
- 💅 **TailwindCSS** — utility-first CSS фреймворк
- 🎭 **shadcn/ui** — современные UI компоненты
- 📊 **Recharts** — библиотека для графиков
- 🎯 **Lucide React** — набор иконок
- 🔌 **Socket.IO Client** — WebSocket клиент

### Backend:
- 🚀 **Next.js API Routes** — серверные эндпоинты
- 🔐 **NextAuth.js** — аутентификация и сессии
- 🗄️ **Prisma ORM** — работа с базой данных
- 🐘 **PostgreSQL** — реляционная СУБД
- 🔌 **Socket.IO Server** — WebSocket сервер
- 📝 **docx** — генерация Word документов
- 📊 **ExcelJS** — экспорт в Excel
- 🔐 **bcryptjs** — хеширование паролей

### Дополнительные библиотеки:
- 📅 **date-fns** — работа с датами
- 🎨 **sharp** — обработка изображений
- 📦 **zod** — валидация данных
- 🪝 **react-hook-form** — управление формами
- 🎯 **class-variance-authority** — стилизация компонентов

---

## 🔐 Безопасность и права доступа

### Аутентификация:
- ✅ JWT токены для API
- ✅ Сессии через NextAuth.js
- ✅ Хеширование паролей (bcrypt)
- ✅ Защита от CSRF
- ✅ HttpOnly cookies

### Авторизация:
- ✅ Role-Based Access Control (RBAC)
- ✅ Проверка прав на уровне API
- ✅ Изоляция данных по компаниям
- ✅ Проверка доступа к проектам:
  - **OWNER/ADMIN** — видят все проекты компании
  - **MANAGER/USER** — видят только свои проекты или где являются участниками

### Защита данных:
- ✅ Валидация входных данных (Zod)
- ✅ Защита от SQL-инъекций (Prisma ORM)
- ✅ Безопасное хранение файлов
- ✅ Ограничение размеров загружаемых файлов

---

## 📊 База данных

### Модели (всего 24):
1. **User** — пользователи системы
2. **Company** — компании/организации
3. **Project** — проекты и объекты
4. **ProjectUser** — участники проектов
5. **ProjectObject** — объекты в проектах
6. **Task** — задачи
7. **TaskAssignment** — назначения задач
8. **TaskDependency** — зависимости между задачами
9. **TaskComment** — комментарии к задачам
10. **Document** — документы
11. **DocumentVersion** — версии документов
12. **DocumentTemplate** — шаблоны документов
13. **Estimate** — сметы
14. **EstimateItem** — позиции смет
15. **Finance** — финансовые операции
16. **Approval** — согласования
17. **ApprovalAssignment** — участники согласований
18. **ApprovalComment** — комментарии к согласованиям
19. **ApprovalAttachment** — вложения в согласованиях
20. **ApprovalHistory** — история согласований
21. **ChatMessage** — сообщения в чате
22. **Notification** — уведомления
23. **Timesheet** — табель учёта времени
24. **NumberingRule** — правила нумерации

### Индексы для оптимизации:
- По компаниям (быстрая фильтрация данных)
- По проектам (связь с основными объектами)
- По датам (сортировка и фильтрация)
- По статусам (поиск активных элементов)
- По ролям пользователей

---

## 🌐 API Архитектура

### RESTful API:
- **GET** — получение данных
- **POST** — создание
- **PUT** — обновление
- **DELETE** — удаление

### Основные эндпоинты (45+):

#### Аутентификация:
- `POST /api/auth/login` — вход
- `POST /api/auth/logout` — выход
- `POST /api/auth/register` — регистрация
- `GET /api/auth/session` — текущая сессия

#### Проекты:
- `GET /api/projects` — список (пагинация, фильтры)
- `POST /api/projects` — создание
- `GET /api/projects/[id]` — детали
- `PUT /api/projects/[id]` — обновление
- `DELETE /api/projects/[id]` — удаление
- `GET /api/projects/[id]/messages` — чат
- `POST /api/projects/[id]/messages` — отправка сообщения
- `GET /api/projects/[id]/members` — участники
- `POST /api/projects/[id]/members` — добавить участника
- `GET /api/projects/[id]/estimates` — сметы проекта
- `POST /api/projects/[id]/estimates` — создать смету

#### Задачи:
- `GET /api/tasks` — список
- `POST /api/tasks` — создание
- `GET /api/tasks/[id]` — детали
- `PUT /api/tasks/[id]` — обновление
- `DELETE /api/tasks/[id]` — удаление
- `GET /api/tasks/[id]/comments` — комментарии
- `POST /api/tasks/[id]/comments` — добавить комментарий

#### Документы:
- `GET /api/documents` — список
- `POST /api/documents` — создание
- `POST /api/documents/upload` — загрузка файла
- `GET /api/documents/[id]/download` — скачивание
- `DELETE /api/documents/[id]` — удаление
- `POST /api/documents/generate` — генерация договора

#### Финансы:
- `GET /api/finance` — список операций
- `POST /api/finance` — создание операции
- `GET /api/finance/[id]` — детали
- `PUT /api/finance/[id]` — обновление
- `DELETE /api/finance/[id]` — удаление

#### Согласования:
- `GET /api/approvals` — список
- `POST /api/approvals` — создание
- `GET /api/approvals/[id]` — детали
- `PUT /api/approvals/[id]` — обновление
- `POST /api/approvals/[id]/respond` — ответ (согласовать/отклонить)
- `GET /api/approvals/[id]/comments` — комментарии
- `POST /api/approvals/[id]/comments` — добавить комментарий
- `GET /api/approvals/[id]/history` — история
- `POST /api/approvals/[id]/attachments` — прикрепить файл

#### Уведомления:
- `GET /api/notifications` — список
- `PUT /api/notifications/[id]/read` — прочитать
- `POST /api/notifications/read-all` — прочитать все
- `DELETE /api/notifications/[id]` — удалить

#### Отчёты:
- `GET /api/reports` — аналитика
- `POST /api/reports/generate` — генерация Excel

#### Пользователи:
- `GET /api/users` — список
- `POST /api/users` — создание
- `GET /api/users/[id]` — профиль
- `PUT /api/users/[id]` — обновление
- `GET /api/users/me` — текущий пользователь
- `GET /api/users/for-approvals` — для согласований

#### Чат:
- `GET /api/chat` — история сообщений
- `POST /api/chat` — отправить сообщение

#### Система:
- `GET /api/health` — проверка работоспособности
- `GET /api/activity` — последняя активность

### WebSocket Events:
- `join-project` — присоединиться к комнате проекта
- `leave-project` — покинуть комнату
- `join-user` — присоединиться к персональной комнате
- `new-message` — новое сообщение
- `user-typing` — пользователь печатает
- `notification` — новое уведомление

---

## 📱 Интерфейс

### Навигация:
- 🏠 **Дашборд** — обзор, статистика, быстрые действия
- 📊 **Проекты** — управление проектами
- ✅ **Задачи** — список задач и канбан-доска
- 📄 **Документы** — документооборот
- 💰 **Финансы** — учёт доходов и расходов
- 🔄 **Согласования** — процессы согласований
- 💬 **Чат** — общение и обсуждения
- 📈 **Отчёты** — аналитика и экспорт
- 👥 **Пользователи** — управление командой
- ⚙️ **Настройки** — настройки компании

### Особенности UI:
- 🎨 Современный дизайн
- 📱 Адаптивная вёрстка (Desktop, Tablet, Mobile)
- 🌙 Готовность к тёмной теме
- ⚡ Быстрая загрузка (Next.js оптимизации)
- 🔔 Реальное время (WebSocket)
- 🎯 Интуитивно понятный интерфейс
- ✨ Плавные анимации и переходы

---

## 🚀 Ключевые особенности функционала (из кода)

### 1. **Автоматические расчёты**
- ✅ **Маржа проектов:** Автоматический расчёт при получении списка (`(income - expenses) / income × 100`)
- ✅ **Финансовые KPI:** Отклонение расходов от плана, доходы по актам
- ✅ **Сметы:** Автоматический расчёт total, totalCost, profit, НДС
- ✅ **Финансовые записи:** Автоматическое создание при создании проекта с бюджетом

### 2. **Фильтрация по ролям**
- ✅ **Проекты:** OWNER/ADMIN видят все, MANAGER/USER - только свои или где участники
- ✅ **Задачи:** OWNER/ADMIN - все, MANAGER - проекты где участник, USER - только назначенные
- ✅ **Согласования:** Фильтрация по создателю, участнику или проекту

### 3. **Автоматизация процессов**
- ✅ **Публикация документов:** Автоматически при одобрении согласования
- ✅ **Создание документов:** Из вложений согласования при одобрении
- ✅ **Уведомления:** Автоматическая отправка при назначении задач, упоминаниях, согласованиях
- ✅ **Финансовые записи:** Автоматическое создание с правильным типом по статусу проекта

### 4. **Безопасность и доступ**
- ✅ **Проверка прав:** Каждый API роут проверяет права через `checkPermission()`
- ✅ **Изоляция данных:** Все запросы фильтруются по `companyId`
- ✅ **Проверка доступа к проекту:** `canUserAccessProject()` перед операциями
- ✅ **Транзакции:** Использование Prisma `$transaction` для атомарности операций

### 5. **Real-time функционал**
- ✅ **WebSocket:** Socket.IO для мгновенной доставки сообщений
- ✅ **Комнаты:** Проектные комнаты и персональные комнаты пользователей
- ✅ **Уведомления:** Мгновенная доставка через WebSocket
- ✅ **Индикатор печати:** Событие `typing` в реальном времени

### 6. **Генерация документов**
- ✅ **Автоматическое заполнение:** 30+ полей из проекта, компании, клиента, сметы
- ✅ **Форматирование:** Локализация дат и сумм (ru-RU)
- ✅ **Сохранение:** Автоматическое создание записи в БД и файла на диске
- ✅ **Публикация:** Автоматическая публикация сгенерированных документов

---

## 📊 Статистика проекта

### Кодовая база:
- **Файлов кода:** ~150+
- **Строк кода:** ~25,000+
- **React компонентов:** ~50+
- **API эндпоинтов:** 45+
- **Модели базы данных:** 24

### Зависимости:
- **npm пакетов:** 50+
- **TypeScript:** 100% покрытие
- **Prisma схема:** 585 строк

### Технологии:
- **Next.js 14** — последняя версия
- **React 18** — с Server Components
- **PostgreSQL** — реляционная БД
- **Socket.IO** — WebSocket
- **Prisma ORM** — type-safe запросы

---

## 🎯 Возможности для развития

### В ближайшем будущем:
1. ✅ Канбан-доска для задач (Drag & Drop)
2. ✅ Диаграмма Ганта для проектов
3. ✅ Мобильное приложение (React Native)
4. ✅ Email-уведомления (Nodemailer)
5. ✅ Интеграция с календарём (Google Calendar, Outlook)
6. ✅ Импорт/экспорт данных (массовая загрузка)
7. ✅ Шаблоны задач и проектов
8. ✅ Автоматизация (триггеры и действия)
9. ✅ Расширенная аналитика (BI Dashboard)
10. ✅ API для интеграций (REST, GraphQL)

### Долгосрочная перспектива:
- 🤖 AI-ассистент для планирования
- 📊 Прогнозирование сроков и бюджетов
- 🔗 Интеграция с 1С, SAP
- 📱 PWA (Progressive Web App)
- 🌍 Мультиязычность
- 🎨 Конструктор отчётов
- 🔐 SSO (Single Sign-On)
- ☁️ Облачное хранилище (S3, Google Drive)

---

## 💼 Преимущества для бизнеса

### Для руководителей:
- 📊 Полный контроль над проектами
- 💰 Прозрачность финансов
- 📈 Аналитика и отчётность
- ⏱️ Экономия времени на рутине

### Для менеджеров:
- 📋 Удобное управление задачами
- 👥 Координация команды
- 📄 Автоматизация документов
- 🔔 Своевременные уведомления

### Для команды:
- ✅ Понятные задачи и сроки
- 💬 Удобное общение
- 📁 Быстрый доступ к документам
- 🎯 Фокус на важном

---

## 📞 Контакты и поддержка

### Техническая информация:
- **Версия:** 1.0.0
- **Лицензия:** Proprietary
- **Node.js:** 18+
- **База данных:** PostgreSQL

### GitHub:
- **Репозиторий:** [Portalactivebiz](https://github.com/SultanSulimanSerj/Portalactivebiz)
- **Коммиты:** Регулярные обновления
- **Ветка:** main

---

## 🎉 Заключение

**Manexa** — это комплексное решение для управления проектами, которое объединяет:

✅ **Управление проектами** с полным контролем  
✅ **Систему задач** с зависимостями  
✅ **Документооборот** с автогенерацией  
✅ **Финансовый учёт** с аналитикой  
✅ **Согласования** с историей  
✅ **Чат в реальном времени** с упоминаниями  
✅ **Уведомления** со звуком  
✅ **Отчёты** с экспортом  

Платформа готова к использованию и продолжает активно развиваться! 🚀

---

*Документация создана: 3 октября 2025 г.*  
*Система находится в активной разработке*

